/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Note: In Tailwind v4, plugins are added via @plugin in CSS
  // This config file is kept for any other Tailwind customizations you might need
}

